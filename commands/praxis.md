---
description: "Run the Praxis adversarial career profile builder. Usage: /praxis build to start the intake wizard, /praxis customize <job-description-url> to tailor a resume."
---

Load the `praxis` skill using the skill tool, then execute the `/praxis` command workflow described in that skill. Pass these arguments to the workflow: $ARGUMENTS